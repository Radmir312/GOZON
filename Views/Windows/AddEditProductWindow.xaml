<Window x:Class="GOZON.Views.Windows.AddEditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавить товар" 
        Height="371" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,1,5,1"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="MinWidth" Value="8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Text="Название товара:*"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Артикул -->
        <TextBlock Grid.Row="2" Text="Артикул (SKU):"/>
        <TextBox Grid.Row="3" x:Name="SKUTextBox" Text="{Binding SKU, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Цена -->
        <TextBlock Grid.Row="4" Text="Цена:*"/>
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PriceTextBox" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Grid.Column="1" Text="руб." Margin="5,5,10,5"/>
        </Grid>

        <TextBlock Grid.Row="6" Text="Минимальное количество:" Margin="0,5,15,0" VerticalAlignment="Top"/>
        <TextBox Grid.Row="6" x:Name="MinQuantityTextBox" Text="{Binding MinQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,28,10,81"/>

        <!-- Описание -->
        <TextBlock Grid.Row="6" Text="Описание:" Margin="2,56,14,0" VerticalAlignment="Top"/>
        <TextBox Grid.Row="6" x:Name="DescriptionTextBox" 
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Height="60"
                 VerticalScrollBarVisibility="Auto" Margin="0,77,10,58" Grid.RowSpan="2"/>

        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Click="SaveButton_Click" IsDefault="True"/>
            <Button Content="Отмена" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
<Window x:Class="GOZON.Views.Main.Windows.AddEditSupplierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавить поставщика" 
        Height="450" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="10,5,5,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Text="Название компании:*"/>
        <TextBox Grid.Row="1" 
                 x:Name="NameTextBox" 
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Контактное лицо -->
        <TextBlock Grid.Row="2" Text="Контактное лицо:"/>
        <TextBox Grid.Row="3" 
                 x:Name="ContactPersonTextBox" 
                 Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Телефон -->
        <TextBlock Grid.Row="4" Text="Телефон:"/>
        <TextBox Grid.Row="5" 
                 x:Name="PhoneTextBox" 
                 Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Email -->
        <TextBlock Grid.Row="6" Text="Email:"/>
        <TextBox Grid.Row="7" 
                 x:Name="EmailTextBox" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Адрес -->
        <TextBlock Grid.Row="8" Text="Адрес:"/>
        <TextBox Grid.Row="9" 
                 x:Name="AddressTextBox" 
                 Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                 Height="60"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="10" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button x:Name="SaveButton" 
                    Content="Сохранить" 
                    Click="SaveButton_Click" 
                    IsDefault="True"/>
            <Button Content="Отмена" 
                    Click="CancelButton_Click" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
<Window x:Class="GOZON.Views.Main.Windows.OutgoingMovementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Отгрузка товара со склада" 
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="10,5,5,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="59*"/>
            <ColumnDefinition Width="321*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Товар -->
        <TextBlock Text="Товар:*" Grid.ColumnSpan="2" Margin="10,0,5,0"/>
        <ComboBox Grid.Row="1" 
                  x:Name="ProductComboBox"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectionChanged="ProductComboBox_SelectionChanged" Grid.ColumnSpan="2" Margin="5,5,5,5"/>

        <!-- Склад -->
        <TextBlock Grid.Row="2" Text="Склад:*" Grid.ColumnSpan="2" Margin="10,0,5,0"/>
        <ComboBox Grid.Row="3" 
                  x:Name="WarehouseComboBox"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectionChanged="WarehouseComboBox_SelectionChanged" Grid.ColumnSpan="2" Margin="5,5,5,5"/>

        <!-- Текущий остаток -->
        <TextBlock Grid.Row="4" Text="Текущий остаток на складе:" Grid.ColumnSpan="2" Margin="10,0,5,0"/>
        <TextBlock Grid.Row="5" 
                   x:Name="CurrentStockTextBlock" 
                   Text="0"
                   FontWeight="Bold"
                   Margin="5,0,5,0" Grid.ColumnSpan="2"/>

        <!-- Текущий остаток -->
        <TextBlock Grid.Row="4" Text="Текущий остаток на складе:"/>
        <TextBlock Grid.Row="5" 
                   x:Name="CurrentStockTextBlock" 
                   Text="0"
                   FontWeight="Bold"
                   Margin="5"/>

        <!-- Количество -->
        <TextBlock Grid.Row="6" Text="Количество:*" Grid.ColumnSpan="2" Margin="10,0,5,0"/>
        <Grid Grid.Row="7" Grid.ColumnSpan="2" Margin="0,0,0,26" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" 
                     x:Name="QuantityTextBox"
                     Text="1"
                     PreviewTextInput="QuantityTextBox_PreviewTextInput"/>
            <TextBlock Grid.Column="1" 
                       Text="шт."
                       Margin="10,0,5,0"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Причина отгрузки -->
        <TextBlock Grid.Row="8" Text="Причина отгрузки:" Grid.ColumnSpan="2" Margin="10,0,5,0"/>
        <TextBox Grid.Row="9" 
                 x:Name="ReasonTextBox"
                 Height="60"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Grid.ColumnSpan="2" Margin="5,29,5,29"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="10" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0" Grid.Column="1">
            <Button x:Name="SaveButton" 
                    Content="Сохранить" 
                    Click="SaveButton_Click" 
                    IsDefault="True"/>
            <Button Content="Отмена" 
                    Click="CancelButton_Click" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>